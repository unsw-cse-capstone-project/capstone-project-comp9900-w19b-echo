import { AddPropertyRequest } from './add-property-request.model';

describe('AddPropertyRequest', () => {
  it('should create an instance', () => {
    expect(new AddPropertyRequest()).toBeTruthy();
  });
});
